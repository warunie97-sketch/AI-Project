export const metadata = {
  title: "AI Writer",
  description: "AI Assistant untuk penulis ✍️✨",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
